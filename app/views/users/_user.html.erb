<li class="user_home">
  <img class="picture" src=<%=user.photo%>>
  <div class="content">
    <%= link_to user.name, user_path(user.id)%>
    <h3> number of posts: <%= user.posts_counter == nil ? 0 : user.posts_counter %></h3>
  </div>
</li>
